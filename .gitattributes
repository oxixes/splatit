* text=auto

# Shell scripts run inside Linux containers, so they must keep LF endings even
# when the repository is checked out on Windows.
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
Dockerfile text eol=lf
